# راهنمای سادهٔ شناخت پروژهٔ لغت‌یار (Lumi)

این سند برای صاحب محصول نوشته شده است؛ کسی که ممکن است خودش کد ننویسد، اما می‌خواهد بداند اپ چگونه ساخته شده، داده‌ها کجا هستند و گزارش‌های فنی چه معنایی دارند.

> **نکتهٔ مهم دربارهٔ این بررسی:** در نسخه‌ای از مخزن که بررسی شد، نسخهٔ داخل فایل‌های اصلی `v2.20.1` بود. اگر در گزارش‌های دیگر نسخه‌هایی مثل `v2.23.0` می‌بینید، باید آن‌ها را با وضعیت فعلی مخزن مقایسه کنید؛ شمارهٔ نسخه به‌تنهایی ثابت نمی‌کند که همان کد در حال اجراست.

## 1. تصویر کلی پروژه (Big Picture)

لغت‌یار را می‌توان مثل یک کتاب آموزشی تعاملی تصور کرد. چند بخش دارد:

- `index.html`: اسکلت و صفحه‌های اپ است؛ یعنی مشخص می‌کند چه صفحه‌هایی وجود دارند: خانه، انتخاب سطح، دسته‌ها، تمرین‌ها و مانند آن. در این پروژه، صفحه‌هایی مثل `screenCategory` و `screenVocabularyLevels` در همین فایل دیده می‌شوند.
- `css/style.css`: ظاهر اپ را کنترل می‌کند؛ رنگ، فاصله، اندازهٔ نوشته، شکل کارت‌ها، دکمه‌ها و نمایش در موبایل.
- `js/app.js`: مغز اجرایی اپ است. داده‌های لغت، دسته‌ها، منطق جابه‌جایی بین صفحه‌ها، مرور، آزمون و ذخیرهٔ پیشرفت در این فایل قرار دارد.
- `build.py`: بسته‌بندی‌کنندهٔ پروژه است. فایل‌های منبع را می‌خواند و از آن‌ها یک فایل نهایی می‌سازد.
- `check_release.py`: بازرس قبل از انتشار است. بررسی می‌کند خروجی مورد انتظار وجود دارد و نشانه‌های مهم نسخهٔ جدید داخل آن دیده می‌شود.
- `dist/index.html`: نسخهٔ آمادهٔ تحویل است. در بیلد، HTML، CSS و JavaScript در یک فایل جمع می‌شوند تا بتوان آن را ساده‌تر به بات تلگرام یا بستهٔ اپ منتقل کرد.
- `css/` و `js/`: پوشه‌هایی هستند که فایل‌های مربوط به ظاهر و منطق را نگه می‌دارند.
- `dist/`: پوشهٔ خروجی است؛ چیزی شبیه جعبهٔ بسته‌بندی‌شدهٔ محصول.
- فایل‌هایی مانند `index-backup.html`، `a2-backup-index.html` و فایل‌های نام‌گذاری‌شده با نسخه، بیشتر نقش نسخهٔ پشتیبان یا عکس‌برداری از وضعیت‌های قبلی را دارند؛ نباید بدون بررسی آن‌ها را منبع اصلی بدانیم.
- پوشه‌هایی مانند `assets/`، `backups/` و `fontwork/` برای فایل‌های کمکی، پشتیبان‌ها یا کارهای مربوط به فونت استفاده می‌شوند.

### منبع و خروجی چه فرقی دارند؟

فایل‌های منبع مثل آشپزخانهٔ رستوران‌اند: مواد اولیه و دستور پخت جدا هستند. `index.html`، `style.css` و `app.js` قابل ویرایش و نگهداری‌اند.

`dist/index.html` مثل غذای بسته‌بندی‌شده برای ارسال است. در آن، ساختار HTML، ظاهر CSS و منطق JavaScript در یک فایل قرار گرفته‌اند.

چرا بیلد لازم است؟ چون تحویل یک فایل واحد برای محیطی مثل بات تلگرام یا APK ساده‌تر است. اگر فقط `app.js` را تغییر دهیم اما بیلد نگیریم، ممکن است خروجی نهایی هنوز نسخهٔ قدیمی را داشته باشد.

در `build.py` سه فایل اصلی خوانده می‌شوند. سپس تگ لینک CSS و تگ اسکریپت JavaScript با محتوای واقعی آن‌ها جایگزین می‌شوند. در پایان، خروجی در `dist/index.html` نوشته می‌شود و یک بررسی ساده هم انجام می‌شود تا کاراکتر خراب وجود نداشته باشد.

## 2. مفهوم «دیتای واژگان» (Data Structure)

هر لغت در `js/app.js` مانند یک کارت در جعبهٔ فلش‌کارت ذخیره شده است. در نمونه‌های پروژه، فیلدهایی مانند این‌ها دیده می‌شوند:

- `word`: خود واژهٔ انگلیسی؛ مثلاً `water`.
- `meaning`: معنی فارسی؛ مثلاً «آب».
- `en`: یک جملهٔ نمونهٔ انگلیسی.
- `fa`: ترجمهٔ فارسی همان جمله.
- `examples`: فهرستی از مثال‌ها؛ هر مثال معمولاً یک متن انگلیسی و ترجمهٔ فارسی دارد.
- `icon`: شکلک یا نشانهٔ تصویری کارت.
- در بخش‌های دیگر داده ممکن است فیلدهایی مانند `id`، سطح، نوع واژه یا اطلاعات دسته‌بندی هم وجود داشته باشد. هنگام تغییر داده باید دقیقاً ساختار همان بخش را بررسی کرد و نباید نام فیلدها را حدس زد.

برای مثال، کارت `water` فقط یک ترجمه نیست؛ واژه، جملهٔ نمونه و چند مثال دیگر را کنار هم نگه می‌دارد. این باعث می‌شود کاربر واژه را در کاربرد واقعی ببیند، نه فقط در یک فهرست خشک.

### CATEGORIES یعنی چه؟

`CATEGORIES` مانند قفسه‌بندی یک کتابخانه است. به‌جای اینکه همهٔ لغت‌ها در یک توده باشند، آن‌ها در دسته‌ها قرار می‌گیرند؛ مثلاً دستهٔ `everyday` با نام فارسی «روزمره».

در گزارش‌ها ممکن است نام‌هایی مثل `Kitchen` یا `pvClothes` ببینید. این نام‌ها معمولاً شناسهٔ یک دسته یا زیردسته‌اند. نام فنی دسته با نام نمایشی آن برای کاربر یکی نیست: مثلاً `pvClothes` ممکن است شناسهٔ داخلی باشد، ولی متن قابل‌نمایش آن «لباس‌ها» باشد.

رابطهٔ دسته و زیردسته را مثل پوشه و زیرپوشه در کامپیوتر در نظر بگیرید:

- دستهٔ اصلی: مثلاً خانه یا زندگی روزمره.
- زیردسته: مثلاً `Kitchen` برای وسایل آشپزخانه.
- شناسهٔ داخلی: نامی که کد با آن دسته را پیدا می‌کند.
- نام نمایشی: متنی که کاربر در اپ می‌بیند.

نکتهٔ محصولی: تغییر نام نمایشی معمولاً کم‌خطرتر از تغییر شناسهٔ داخلی است. شناسه ممکن است در پیشرفت کاربر، فیلترها یا لینک‌های داخلی استفاده شده باشد.

## 3. مفهوم «پیشرفت کاربر» (Progress Tracking)

وقتی کاربر یک لغت را می‌خواند، مرور می‌کند یا یادگرفته‌شدن آن را ثبت می‌کند، اپ باید این اطلاعات را جایی نگه دارد تا با بستن صفحه از بین نرود.

در یک اپ تک‌صفحه‌ای مثل لغت‌یار، چنین اطلاعاتی معمولاً در حافظهٔ محلی مرورگر، یعنی `localStorage`، ذخیره می‌شود. این فضا را مثل کشوی شخصی کاربر در همان دستگاه تصور کنید؛ اپ می‌تواند دفعهٔ بعد کشو را باز کند و ببیند چه چیزهایی قبلاً انجام شده‌اند.

پیشرفت ممکن است شامل این موارد باشد:

- چه لغت‌هایی دیده یا مرور شده‌اند.
- چه لغت‌هایی برای مرور بعدی آماده‌اند.
- تعداد مرورها یا وضعیت یادگیری.
- آخرین زمان مرور یا موعد مرور بعدی.
- انتخاب سطح یا مسیر یادگیری.

### کلید پیشرفت (progress key)

کلید پیشرفت، نام یکتایی است که اپ با آن سابقهٔ یک مورد را پیدا می‌کند؛ مثل شمارهٔ پروندهٔ یک دانش‌آموز.

اگر کلید یک لغت بر اساس شناسهٔ دسته و نام واژه ساخته شود، مثلاً ترکیبی از دسته و واژه، تغییر دسته می‌تواند باعث شود اپ همان لغت را یک مورد تازه بداند. نتیجه ممکن است این باشد که کاربر ناگهان لغتی را «یادنگرفته» ببیند، یا سابقهٔ قبلی به کارت جدید وصل نشود.

پس هنگام جابه‌جایی یک لغت از دستهٔ `Kitchen` به دستهٔ دیگر، باید پرسید:

1. کلید پیشرفت بر اساس چه چیزی ساخته می‌شود؟
2. آیا شناسهٔ پایدار برای خود لغت وجود دارد؟
3. آیا داده‌های قدیمی باید به کلید جدید منتقل شوند؟
4. آیا برای کاربران قبلی migration لازم است؟

بهترین حالت این است که هویت اصلی لغت ثابت بماند و دسته فقط یک ویژگی قابل‌تغییر باشد.

## 4. مفهوم سطح‌بندی CEFR

`CEFR` یک چارچوب شناخته‌شده برای توصیف سطح زبان‌آموزی است. در این پروژه سطح‌ها به شکل زیر نمایش داده می‌شوند:

- `A1`: شروع مسیر؛ واژه‌های پایه و روزمره.
- `A2`: کمی بالاتر؛ واژه‌های بیشتر و مکالمهٔ کاربردی‌تر.
- `B1`: سطح متوسط؛ متن و مکالمهٔ گسترده‌تر.
- `B2`: سطح بالاتر؛ محتوای عمیق‌تر.

در صفحهٔ انتخاب سطح پروژه، A1 و A2 در نسخهٔ بررسی‌شده قابل انتخاب بودند و B1 و B2 با وضعیت «به‌زودی» نمایش داده می‌شدند.

نکتهٔ مهم: CEFR فقط یک برچسب تزئینی نیست. اگر لغتی به سطح A1 نسبت داده شده، باید در انتخاب محتوا، تمرین‌ها، آزمون‌ها و ترتیب بازشدن مسیر اثر داشته باشد. بنابراین هنگام اضافه‌کردن لغت باید پرسید سطح آن کجا ثبت می‌شود و چه بخش‌هایی از اپ از آن استفاده می‌کنند.

## 5. فرآیند «بیلد و انتشار» (Build & Release)

### `python3 build.py` چه می‌کند؟

این دستور مثل دستگاه بسته‌بندی است:

1. `index.html` را می‌خواند.
2. `css/style.css` را می‌خواند.
3. `js/app.js` را می‌خواند.
4. جای اتصال CSS و JavaScript را با محتوای واقعی آن‌ها عوض می‌کند.
5. فایل یکپارچهٔ `dist/index.html` را می‌سازد.
6. بررسی می‌کند کاراکتر خراب `�` در خروجی نباشد.

پس بعد از تغییر در منبع، خروجی نهایی باید دوباره ساخته شود.

### `check_release.py` چه می‌کند؟

این فایل تست کامل محصول نیست؛ بیشتر یک چک‌لیست خودکار برای انتشار است. در نسخهٔ بررسی‌شده، انتظار داشت نسخهٔ `v2.20.1` در خروجی وجود داشته باشد و چند متن مهم هم دیده شود؛ از جمله انتخاب سطح A1 یا A2 و پیام‌های مربوط به مرور و کامل‌شدن مأموریت‌های روزانه.

اگر خروجی پیدا نشود، نسخه درست نباشد یا یکی از متن‌های مورد انتظار داخل فایل نباشد، اسکریپت خطا می‌دهد و پیشنهاد می‌کند اول `build.py` اجرا شود.

بنابراین «موفق شدن check_release» به معنی بی‌نقص بودن کل اپ نیست؛ فقط یعنی چند شرط مشخص برای خروجی بررسی شده‌اند.

### نسخه مثل `v2.23.0` یعنی چه؟

شمارهٔ نسخه مثل برچسب روی جعبهٔ محصول است. معمولاً سه بخش دارد:

- عدد اول: تغییر بزرگ یا ناسازگاری مهم.
- عدد دوم: قابلیت جدید.
- عدد سوم: اصلاح کوچک یا رفع باگ.

این قرارداد همیشه مطلق نیست، اما برای گفت‌وگو مفید است. نسخه کمک می‌کند بدانیم گزارش، فایل خروجی، اسکرین‌شات و تجربهٔ کاربر دربارهٔ کدام وضعیت محصول صحبت می‌کنند.

## 6. فرهنگ اصطلاحات تکراری در گزارش‌ها (Glossary)

- **`node --check`**: بررسی می‌کند کد JavaScript از نظر شکل نوشتاری قابل‌فهم است یا نه؛ مثل بررسی دستور زبان یک متن، نه بررسی درست‌بودن معنی آن.
- **`tsc --noEmit`**: ابزار TypeScript کد را بررسی می‌کند، اما فایل خروجی جدید نمی‌سازد. در پروژه‌ای که JavaScript ساده دارد ممکن است اصلاً کاربرد مستقیم نداشته باشد؛ باید دید آیا فایل TypeScript یا تنظیمات مربوط وجود دارد.
- **`auditVocabularyData`**: نام یک بررسی برای کنترل کیفیت داده‌های واژگان است؛ مثلاً پیدا کردن لغت بدون ترجمه، مثال ناقص، شناسهٔ تکراری یا سطح نامعتبر.
- **`lint`**: بازبینی خودکار سبک و خطاهای رایج کد؛ مثل ویراستار خودکار.
- **`commit`**: ثبت یک بستهٔ مشخص از تغییرات در تاریخچهٔ Git؛ مثل ذخیرهٔ یک نقطهٔ قابل‌برگشت با توضیح.
- **`git status` / `git diff`**: اولی می‌گوید چه فایل‌هایی تغییر کرده‌اند؛ دومی دقیقاً نشان می‌دهد چه خط‌هایی عوض شده‌اند.
- **staging**: آماده‌کردن انتخابی تغییرات برای commit؛ مثل گذاشتن چند وسیله در جعبه قبل از بستن آن.
- **homograph**: دو واژه با شکل نوشتاری یکسان اما معنی یا کاربرد متفاوت؛ مثلاً یک واژهٔ انگلیسی که در دو معنی جدا به کار می‌رود. در دادهٔ واژگان باید مراقب بود یک کارت، معنی‌ها و مثال‌های اشتباه را با هم قاطی نکند.
- **migration**: انتقال داده‌ها یا ساختار قدیمی به شکل جدید؛ مثلاً تبدیل کلیدهای پیشرفت قدیمی پس از تغییر شناسهٔ دسته.
- **regression**: خراب‌شدن قابلیتی که قبلاً درست کار می‌کرد؛ مثلاً اضافه‌کردن یک دسته باعث شود مرور امروز دیگر چیزی نشان ندهد.
- **refactor**: مرتب‌کردن داخل کد بدون تغییر عمدی در رفتار محصول؛ مثل مرتب‌کردن انبار بدون تغییر کالاها.
- **build / بیلد**: تبدیل منابع جداگانه به بستهٔ قابل‌تحویل؛ در این پروژه خروجی اصلی `dist/index.html` است.
- **debug / دیباگ**: پیدا کردن علت خطا و رفع آن؛ نه صرفاً پنهان‌کردن علامت خطا.

## 7. چطور یک گزارش فنی را خودم بخوانم و ارزیابی کنم؟

وقتی گزارش فنی می‌خوانید، اول دنبال این پنج چیز بگردید:

1. **هدف دقیق:** چه چیزی قرار بود تغییر کند؟ مثلاً «اضافه‌شدن زیردستهٔ `Kitchen`» با «بازطراحی کامل دسته‌ها» یکی نیست.
2. **فایل‌های درگیر:** آیا گزارش نام فایل واقعی مثل `js/app.js`، `index.html` یا `build.py` را گفته است؟
3. **اثر روی کاربر:** کاربر چه چیزی می‌بیند یا چه چیزی بهتر می‌شود؟
4. **آزمون و شواهد:** آیا فقط نوشته «تست شد» یا دستور، نتیجه و موارد بررسی‌شده را هم آورده است؟
5. **وضعیت انتشار:** آیا `build.py` اجرا شده؟ آیا `check_release.py` موفق بوده؟ آیا نسخهٔ خروجی با نسخهٔ گزارش یکی است؟

### علامت‌های هشداردهنده

- گزارش می‌گوید «همه‌چیز درست است» اما هیچ نتیجهٔ قابل‌بررسی ندارد.
- نسخهٔ منبع و نسخهٔ `dist/index.html` با هم هماهنگ نیستند.
- دسته‌بندی تغییر کرده، اما دربارهٔ کلیدهای پیشرفت حرفی زده نشده است.
- تعداد زیادی فایل پشتیبان تغییر کرده‌اند، بدون اینکه دلیلشان روشن باشد.
- گزارش از «رفکتور» حرف می‌زند ولی رفتار کاربر هم تغییر کرده است.
- تست فقط `node --check` بوده؛ این تست خطاهای منطقی، دادهٔ اشتباه یا تجربهٔ بد کاربر را پیدا نمی‌کند.
- نوشته شده «انتشار آماده است» ولی معلوم نیست کدام فایل واقعاً به بات تلگرام یا APK داده خواهد شد.

### سؤال‌های خوب از مدل یا توسعه‌دهنده

به‌جای اینکه فقط بپرسید «درست شد؟»، بپرسید:

- دقیقاً کدام فایل‌ها تغییر کردند و چرا؟
- اگر کاربر قبلاً پیشرفت داشته باشد، چه اتفاقی برای سابقه‌اش می‌افتد؟
- آیا این تغییر روی A1، A2 یا دسته‌هایی مثل `Kitchen` و `pvClothes` اثر دارد؟
- چه چیزی تست شد و چه چیزی هنوز تست نشده است؟
- آیا خروجی `dist/index.html` دوباره ساخته شده است؟
- نسخهٔ واقعی خروجی چیست و از کجا تأیید شده؟
- اگر این تغییر خراب شود، چطور می‌توانیم سریع به نسخهٔ قبلی برگردیم؟

## پرسش‌هایی برای ادامهٔ یادگیری

1. در لغت‌یار، دقیقاً کلید یکتای هر لغت چیست و آیا مستقل از دسته است؟
2. پیشرفت کاربر در کدام کلیدهای `localStorage` ذخیره می‌شود؟
3. مسیر کامل کلیک کاربر از صفحهٔ خانه تا نمایش یک لغت چگونه است؟
4. `CATEGORIES` چگونه به صفحهٔ نمایش دسته‌ها وصل می‌شود؟
5. اگر یک لغت از `Kitchen` به `pvClothes` منتقل شود، چه migration لازم است؟
6. کدام تست‌ها دادهٔ واژگان را بررسی می‌کنند و کدام تست‌ها رفتار رابط کاربری را؟
7. چرا نسخهٔ نوشته‌شده در `check_release.py` باید با نسخهٔ خروجی هماهنگ باشد؟
8. چه تفاوتی میان فایل منبع، خروجی `dist` و فایل‌های backup وجود دارد؟
9. برای اضافه‌کردن یک سطح جدید مثل B1، کدام فایل‌ها و منطق‌ها باید تغییر کنند؟
10. چه شواهدی لازم است تا بگوییم یک نسخه واقعاً آمادهٔ انتشار است؟
